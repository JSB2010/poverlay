# Samples

Place local GPX/video test files here.

Notes:
- Large video files (`.MP4`/`.MOV`) are intentionally ignored by git.
- Text/small sample assets (for example GPX files) can be committed if desired.
