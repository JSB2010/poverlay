# Centralized env now lives at repository root in `.env.example`.
# Keep this file only for backward-compatibility with existing tooling.
# Recommended:
#   cp .env.example .env
# and run apps through `./scripts/run.sh`, `./scripts/run-web.sh`, or `./scripts/run-api.sh`.
