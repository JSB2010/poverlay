# Site Configuration
NEXT_PUBLIC_SITE_URL=https://poverlay.com

# Optional. Use when calling FastAPI directly from the browser.
# NEXT_PUBLIC_API_BASE=http://localhost:8787

# Used by Next.js server rewrites in dev/prod when requests hit /api/*.
API_PROXY_TARGET=http://127.0.0.1:8787

# Max request body size for Next.js proxying /api/* uploads.
# Default in code is 1024mb.
# NEXT_PROXY_CLIENT_MAX_BODY_SIZE=1024mb
