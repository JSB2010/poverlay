fastapi==0.128.5
uvicorn==0.40.0
python-multipart==0.0.22
gopro-overlay==0.129.0
firebase-admin==7.1.0
boto3>=1.34,<2
google-cloud-firestore>=2.11,<3
sib-api-v3-sdk>=7.0,<8
# geotiler (pulled by gopro-overlay) imports pkg_resources at runtime.
# pkg_resources is removed in setuptools>=81, so keep this upper bound until
# upstream no longer depends on it.
setuptools<81
